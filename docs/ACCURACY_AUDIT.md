# RE Accuracy Audit — coverage matrix

Goal: 100% behavioral accuracy to the original SNES ROM. Every function/subsystem
verified against the ASM (line-by-line logic diff) and, where isolatable, the
sf-oracle differential harness (65816 `call`/`call_near`, GSU `Gsu::run`).

Status legend: VERIFIED (oracle-proven or line-diffed vs ASM, regression test
kept) · FIXED (bug found + fixed + test) · PARTIAL (some fns verified) ·
UNVERIFIED (not yet audited) · N/A (no ROM counterpart, e.g. HD glue).

| Subsystem | Rust | ROM source | Status | Evidence |
|---|---|---|---|---|
| Core trig/tables (mulslog, SINTAB/COSTAB, rotate_8*/16*) | sf-core snes_trig (+ sf-strat re-export) | MACROS.INC, STRATROU | VERIFIED (reachable range) | audit_trig.rs / fuzz_rotate8; path spawn Roffs via strat_roffs_full_scaled (tick 184) |
| perc56/62/75/87/93 | sf-strat common | STRATROU.ASM:2494 | VERIFIED | audit_trig.rs |
| gen_vecs 2D/3D/side/front | sf-strat common | STRATROU.ASM alvelvecs/n3dvecs/sidevecs/frontvecs | VERIFIED | audit_trig.rs, gen_3dvecs.rs |
| speed_to | sf-strat common | STRATROU.ASM sr_speedto | VERIFIED (reachable) | speedto.rs; ≥128 wrap documented |
| chase / chase8 / chase_proportional | sf-strat common | STRATMAC adiv2 | FIXED (i16::MIN overflow) | commit a3414b9; adiv2 toward-zero verified |
| dist_xz | sf-strat common | STRATROU.ASM xzdiffs_l:1796 | FIXED (Manhattan→scaled-Euclid) | audit_coldet.rs bit-exact |
| angle_xz / Xangle* | sf-core aim_angle (+ strat re-export) | Yanglexy/Xanglexy/Xanglexabs | FIXED (abs elev=Manhattan; obj elev=xzdiffs; was hypot/scaled-for-abs) | aim_angle.rs + anglexy_compose; GSU ±1 LSB |
| RNG | sf-strat common | RANDOM SWB chain | VERIFIED | random.rs bit-exact |
| apply_velocity / add_to_pos | sf-strat common | STRATROU addalvecs/addvecs | VERIFIED | apply_vel.rs |
| init_objvars | sf-game obj.rs | STRATROU init_objvars_l:2311 | FIXED (realobj/inviewpl/animframe/colframe/type_) | audit_boss.rs ROM dump |
| obj alloc/free/kill lists | sf-game obj.rs | OBJ/MAIN FmtFreeLst | FIXED (kill_all order) | audit_coldet.rs |
| do_coll | sf-game coldet | do_coll_l $1FD252 | VERIFIED | do_coll.rs |
| coldet_run (filters, immunity, collcount) | sf-game coldet | COLDET.ASM 172/518/523/829 | FIXED (all-active collision reset, collcount seed, immunity sentinel, category filter, double dispatch; removed non-ROM friend skip) | unit_core + pcbox_routing |
| player collision routing (pcbox objects) | sf-game coldet + sf-strat player | COLBOXES.ASM:20-22; PSTRATS.ASM:91-248/3279-3359 | VERIFIED (exact ship-owned 10×10×20 body + rotated 5×5×10 wings; HF1/2/3 routing; body/wing/wire damage and break/death flow) | pcbox_routing 7/7 + player_damage_sfx 10/10 + gameplay_collision_control |
| Player movement (steering, banking, decays, clamps, bounds) | sf-strat player | PSTRATS 2280-2736 | VERIFIED (boost/brake pulse+noctrl High #1 closed tick 197) | audit_player.rs + player_bounds.rs + player_boost_brake.rs |
| Player weapons (cadence, caps, single/double/beam) | sf-strat player | PSTRATS playerfire_srou:2836 | VERIFIED | line-diff (session log) |
| SF2 player blaster and charge | sf2-game typed mission/object state | Retail six-pilot charge table and live first-sortie object trace | FIXED (rapid shot, held orb, and charged projectile are distinct; pilot thresholds and damage restored) | ROM table differential + Mesen hold/early-release/charged-release traces + native lifecycle tests |
| SF2 player damage and continue | sf2-game typed damage/game-over state + sf-app/sf-render/sf-audio integration | Retail hostile-projectile collision, hit recovery, destruction, Game Over, and reserve-pilot handoff | FIXED (exact asymmetric collision box, attack power, zero-shield survival, 40-frame recovery, 280-frame destruction, 400-frame prompt, 172-frame return, typed Yes/No flow, reserve promotion, dedicated native music/UI path) | deterministic Mesen boundary probes + retained timing fixture + exact native collision faces and end-to-end continue regression + offscreen renderer and architecture gates |
| SF2 music routing | sf-app typed mode/mission router + sf-audio semantic PCM catalog | Live retail audio-program selection across every campaign sequence | FIXED (pilot selection, all 17 typed mission visits, Game Over/Results, and ending select their own verified soundtrack families instead of reusing map/title music) | 10,038 captured WRAM states + audible oracle renders + byte-identical 180-second regeneration + exhaustive routing tests + real hidden-app startup |
| SF2 player combat audio | sf2-game typed audio events + sf-app bank/pilot router + sf-audio semantic PCM catalog | Live retail player-weapon, charge, and flight-engine commands across all mission programs and pilot data | FIXED (rapid and charged release events, building/ready charge transitions, and looping flight engine select the exact mission-bank and active-pilot variant) | Mesen command/timing trace + deterministic 8-bank by 6-pilot by 5-cue oracle render + exhaustive routing/layer tests + shipping architecture gates |
| SF2 hostile projectile audio | sf2-game typed hostile-projectile allocation event + sf-app/sf-audio semantic PCM routing | Retail hostile-laser cue at every successful hostile shot creation | FIXED (all scripted and live hostile-projectile families queue a named event only after allocation; simultaneous shots are retained) | object-correlated Mesen trace + deterministic 8-bank by 6-pilot render + byte-identical 48-file regeneration + exact one-shot/two-shot event regressions + shipping architecture gates |
| SF2 capital-craft engine audio | sf2-game typed spatial-sound state + sf-app/sf-audio positional layer | Retail capital-engine distance and stereo selections across live craft encounters | FIXED (the nearest active capital craft selects one of four semantic distance bands and three stereo positions; source changes restart the independent positional loop without interrupting music, player engine, charge, or effects) | object-correlated Mesen trace + deterministic 8-bank by 6-pilot by 12-cue render + nearest-source and mixer-layer regressions + real hidden-app startup + shipping architecture gates |
| SF2 re-engagement mission radio | sf2-game typed mission-message state + sf-render native presentation + sf-audio semantic effects | Retail Slippy guidance request, opening/closing animation, portrait poses, text panel, and cues | FIXED (the second-sortie guidance line appears at the exact four-frame boundaries with captured Slippy artwork, authored mouth sequence, localized panel, and native open/close PCM) | deterministic save-state replay + all 39 open portrait boundaries + exact generated-crop hashes + locked GPU frame + byte-identical 8-bank by 6-pilot by 2-cue render + shipping architecture gate |
| SF2 pilot craft forms | sf2-game typed pilot/object state | Retail active-flight, transformation, and walker tables | FIXED (three active-flight classes; Select-driven two-stage round trip; exact class-specific meshes, frame direction/endpoints, sampled timing, and destination forms; first-person handoff retains craft identity without drawing the player body) | six retail descriptor records + live shape/frame write trace + Mesen round-trip capture + native/render-boundary tests |
| SF2 Walker controls | sf2-game typed player/walker state | Retail ground-form input and player movement routines | VERIFIED (pilot-specific forward motion, turn spring/easing, jump wind-up, delayed ascent, gravity, collision landing, camera, and blaster use flat typed state) | ROM pilot tables and vertical-pipeline instruction slices + Mesen L/R/Up/Y stage traces + exact released/held per-tick native regressions + sf2_walker_motion oracle tests |
| SF2 path contact collision | sf2-game oracle-only typed path host + sf2-data collision catalog | Retail downward collision refresh and three-way contact classifier | VERIFIED (479 bounds colliders; all 98 compound-collider shapes, 78 profiles, 355 groups, 407 animation records, and 273 polygon footprints; rotated surfaces and non-solid link fallback) | exact generated catalog + accepted isolated retail differential for every record + simple X/Y/Z edge matrix + rotated polygon and fallback regressions |
| SF2 opening-sortie fighter AI | sf2-game typed fighter/task state | Retail first-sortie fighter movement, altitude, maneuver, fire, and cooperative task services | VERIFIED for the certified frame 480–2448 encounter window (native dynamics replace the sampled fighter-pose override through the final coherent frame) | live per-operation Mesen trace + exact every-four-retail-frame poses from first shot through frame 2448 + shared-random boundary states + shipping architecture gate |
| SF2 opening-sortie capital craft | sf2-game typed capital-flight state | Retail pursuit, player-facing, banked movement, altitude wave, and weapon-aim tasks | VERIFIED for the certified frame 900–2448 encounter window (native dynamics replace the sampled capital-pose override through the final coherent frame) | compact Mesen logic trace + generator replay over all 388 four-frame poses + exact native runtime regression + shipping architecture gate |
| SF2 first-reengagement fighter AI | sf2-game typed reengagement fighter state | Retail second-sortie entry, banked movement, altitude wave/centering, maneuver, and weapon-pitch tasks | VERIFIED for both complete fighter lifetimes at frames 68–1460 (sampled shipping tracks removed) | compact Mesen logic fixture + independent semantic replay over all 449 visible poses + exact native runtime regression + shipping architecture gate |
| SF2 first-reengagement capital craft | sf2-game typed capital-flight state | Retail second-sortie pursuit, player-facing, banked movement, altitude wave, and weapon-aim tasks | VERIFIED for both complete capital-craft lifetimes at frames 68–6104 (sampled shipping tracks removed) | compact Mesen logic fixture + independent semantic replay over all retained poses + exact native runtime regression + shipping architecture gate |
| SF2 first-reengagement hostile projectiles | sf2-game typed projectile flight state | Retail target contraction, immediate/smoothed aiming, flight, and cruise tasks | VERIFIED for all 33 complete lifetimes at frames 588–5724 (sampled shipping tracks removed) | compact Mesen logic fixture + independent flat-state replay over all 942 retained poses + exhaustive native runtime regression + shipping architecture gate |
| SF2 three-fighter-interception fighter AI | sf2-game typed interception-flight state | Retail entry/combat movement, speed control, banking, player-facing, altitude wave, combat corridor, weapon aim, and cooperative continuations | VERIFIED for all 3 complete lifetimes at frames 0–3116 (sampled shipping tracks removed) | compact Mesen logic fixture + independent flat-state replay over all 1503 retained poses + exhaustive native runtime regression including hidden presentation and departures + shipping architecture gate |
| SF2 three-fighter-interception hostile projectiles | sf2-game shared typed projectile flight state | Retail target contraction, immediate/smoothed aiming, flight, and cruise tasks | VERIFIED for all 8 complete lifetimes at frames 1016–2924 (sampled shipping tracks removed) | deterministic save-state replay aligned to all 832 sortie samples + compact Mesen logic fixture + independent flat-state replay over all 277 retained projectile poses + exhaustive native runtime regression + shipping architecture gate |
| SF2 campaign-missile interception targets | sf2-game typed missile-flight state | Retail initialization, straight flight, randomized one-axis steering, spin, cooperative movement continuations, visibility, and departure | VERIFIED for all 3 complete lifetimes at frames 64–2568 (sampled shipping tracks removed) | accepted sortie trace + save-state operation stream proven identical across all 661 shared samples + compact semantic fixture + independent flat-state replay over all 1817 retained poses + exhaustive native runtime regression + shipping architecture gate |
| SF2 Pigma-duel hostile projectiles | sf2-game shared typed projectile flight state | Retail target contraction, immediate/smoothed aiming, flight, cruise, and cooperative movement continuation | VERIFIED for all 3 complete lifetimes at frames 772–988 (sampled shipping tracks removed) | deterministic save-state replay aligned to the accepted 420-sample sortie + compact Mesen logic fixture + independent flat-state replay over all 120 retained projectile poses + exhaustive native runtime regression including the split-operation boundary + shipping architecture gate |
| SF2 Pigma-duel rival flight | sf2-game typed Pigma flight state | Retail approach steering, speed changes, player-facing attack, altitude hold, second pass, deceleration, escape, and cooperative task timing | VERIFIED for the complete visible lifetime at frames 36–1224 and departure at 1228 (sampled shipping track removed) | deterministic save-state operation trace + reproducible compact semantic import + exact native regression over all 298 retained poses and departure + shipping architecture gate |
| SF2 Leon-duel rival flight | sf2-game typed Leon flight state | Retail approach steering, speed changes, combat-altitude hold, roll leveling, and cooperative movement continuation | VERIFIED for the complete visible lifetime at frames 52–660 and departure at 664 (sampled shipping track removed) | accepted sortie trace + isolated operation trace matching every retained rival pose through frame 660 + reproducible compact semantic import + exact native regression over all 153 retained poses and departure + shipping architecture gate |
| SF2 Leon-duel projectile ownership | sf2-game player weapon state and hostile-projectile allocation | Player shots remain player-owned; hostile shots begin only when Leon fires | VERIFIED across the fast-kill route and extended 0–1880-frame combat capture (six complete hostile-projectile lifetimes; sampled shipping tracks removed) | accepted raw sortie records + extended deterministic save-state replay + compact semantic importer over all 132 retained hostile-projectile poses + exhaustive native regression + shipping architecture gate |
| SF2 final-campaign rival flights | sf2-game shared typed final-rival flight state | Retail entrance, approach steering, speed changes, combat-altitude limits, roll leveling, live player-facing attack, departure steering, and cooperative operation timing | VERIFIED for the final pursuer at frames 56–892 with departure at 896 and Wolf blockade at frames 60–952 with departure at 956 (both sampled shipping tracks removed) | deterministic save-state operation replay byte-identical across the accepted 4100-frame segment + reproducible compact semantic import + exact native regressions over all 209 final-pursuer and 224 Wolf poses, hidden presentation, and departures + shipping architecture gate |
| SF2 final-campaign hostile projectiles | sf2-game shared typed projectile flight state | Retail target contraction, immediate/smoothed aiming, homing, aim correction, cruise, and live player-position timing | VERIFIED for all 3 final-pursuer lifetimes at frames 728–836 and all 3 Wolf-blockade lifetimes at frames 788–952 (all six sampled shipping tracks removed) | deterministic save-state replay reproducing the accepted 4100-frame encounter records + reproducible compact semantic imports + independent flat-state replay over all 49 final-pursuer and 89 Wolf retained poses + exhaustive native runtime regressions + shipping architecture gate |
| Player spawn defaults (outdist etc.) | sf-strat player | MAPMACS/GSTRATS | FIXED (outdist=120 seed removed) | audit_player agent + re-bless |
| Camera matrix (rotation) | sf-render transform | GSU mcrotmatzxy16 MWCROT.MC | FIXED (ZXY, was ZYX+flipped yaw) | gsu_rotmat.rs Δ=0 |
| Projection/FOV | sf-render transform | GSU mdo_project MOBJ.MC:5156, cscrc=112 | FIXED (60°→47.2°, scale 256) | derivation in commit 2d368da; cross-validated by bg2d focal=256 |
| 2D-bg scroll (horizon coupling) | sf-render bg2d | GSTRATS.ASM calcbgscroll_l:3190 | FIXED (ROM-exact: linear -6px/pitch + clamp + BGS base; dropped tan() + the +18 fudge; horizontal yaw·8 verified) | commit + gl_runtime golden; frame-verified |
| Camera view source (getview_l) | sf-game camera | GAME.ASM:6-113 + TOOBJ 133-147 | NORM outv* + `rotate_16*` pull-back + TOOBJ aim_angle | audit_player.rs + `toobj_lookat_*` + `pullback_*` |
| Map-VM spawn opcodes (QOBJ/OBJ8/DOBJ/MAPOBJ/MOTHER/OBJZROT coords) | sf-game game.rs | WORLD.ASM | VERIFIED | audit_mapvm.rs 5 ROM-diff tests |
| Map-VM non-spawn opcodes (71-op table) | sf-game game.rs/world.rs | WORLD.ASM + MAIN.ASM fadepalto_l | FIXED (maploop count-1, REMOVE one-match+ref-clears, SETBGM hp0 guard, WAIT2-zero, SETVAROBJ-invalid, SETBGM alias); MOTHER submap PORTED (bemother oracle bit-exact, asteroid waves restored); FADETOSEA/GROUND VERIFIED (typed flat state; exact SEA/GROUND source and background row-4 high-to-low word walk; polygon row 7 remains independent) | audit_mapvm2 18/18 + audit_mother 2/2 + renderer Fortuna ownership tests |
| SF1 opening screen wipes | sf-core typed aperture state + sf-game catalog/shell + sf-render overlay | MAPMACS.INC `initlevel`/`wipein`; MDATA.MC `mstarwipe`/`mscramwipe`; MBUMWIPE.MC | FIXED (all 19 route-map operands and Training mapped; exact 16-record star and 12-record horizontal apertures; Corneria's initial star and later launch-corridor reveal remain distinct; source script pointers replaced by flat semantic state) | source-boundary catalog test + integer scanline geometry tests + real GPU closed/midpoint/final-frame pixel checks + unattended routes + 107/107 retail coexec + architecture gate |
| SF1 player view cycle and cockpit transitions | sf-core typed view enums + sf-game flat state/dispatcher + sf-strat transition strategies | GILESALC.INC `spfm_*`/`spfmo_*`; GSTRATS.ASM `init_strats_l`/`changeviewmode_l`; BGS.ASM `pstrat`; PISTRATS/GASTRATS/GA2STRAT/MAP3_6 | FIXED (Select edge and every retail gameplay gate restored; all playable opening/runtime background declarations mapped; exterior distances and authored enter/exit transitions dispatch through named callbacks; scripted warp, carrier, colony, and map exits no longer stop at a raw mode write) | source-boundary enum/catalog tests + exact gate/sound/cycle tests + registered transition and scripted-exit tests + unattended routes + retail coexec + architecture gate |
| SF1 player death, crash, and terminal explosion | sf-core typed view/red-fill state + sf-game flat flags/object references + sf-strat player transitions + sf-render additive overlay | PSTRATS.ASM `playerdead_Istrat`/`playeroutofcock_Istrat`/`playerdead_strat`/`pexplode_Istrat`; GSTRATS.ASM fade delay; TRANS.ASM `redfillscreen_circle` | FIXED (cockpit death runs the authored 23-count ejection before the ordinary crash; the terminal transition creates an inert player/camera anchor one velocity step ahead, transfers the camera and circle references, replaces the ship with the generic explosion lifecycle, creates the large-particle explosion, removes the shadow, retains dying while setting dead, decrements one life, expands the exact typed additive-red circle, waits 20 ticks, and completes the native black fade before dispatch; no source command pointer or generic memory enters shipping state) | exact object-graph/callback/countdown/audio/life regression + typed red-circle unit tests + real additive GPU readback + exact fade-boundary shell regression + full strategy package + all three unattended routes + 107/107 retail coexec + architecture gate |
| SF1 generic object explosions | sf-core generated semantic shape metrics + sf-game typed visual kind + sf-strat explosion lifecycle + sf-render camera-facing scaled sprites | EXPSTRAT.ASM `explode_Istrat`/`explode_Icont`/`explode_strat`/`Lexplode_strat`; MDRAWLIS.MC `mssprite`; source explosion ShapeHdrs | FIXED (in-view destruction now removes attached fire, preserves special scoring and no-sound/no-polygon gates, classifies the source visual extent at 64/128/256, selects all seven authored sprite/debris meshes, applies the exact signed size adjustment, consumes the two authored random rotations, and retains independent 4/6/8-frame sprite and 12-frame polygon lifetimes; oversized polygons use the source half-rate tick; scaled sprites are typed flat object state and always face the camera) | exact four-class selection test + lifecycle/object-field/audio/no-polygon regressions + 473-shape metrics coverage + rotated-camera matrix proof + real GPU size readback + corrected source-boundary boss/enemy traces + full strategy package + architecture gates |
| SF1 hyperspace streaks | sf-strat flat emitter/object state + sf-render generated mesh catalog | PISTRATS.ASM `hyperspace_Istrat`/`hyperspaceout_Istrat`/`hyper_Istrat`; SHAPES2.ASM `hyper*`; MOBJ.MC `mfaces` | FIXED (both initializers execute their authored first body immediately; the emitter consumes the exact five random bytes, writes independent signed screen-space coordinates, applies the roll-only offset and view-center displacement, and defers streak motion to its installed strategy; the outgoing sequence selects all four authored meshes while shrinking over 64 ticks; two-point faces bypass polygon visibility exactly as the retail renderer does) | exact initializer/random-stream/coordinate/phase-boundary regressions + exact mesh topology + deterministic 473-shape generation + complete strategy package + all three unattended routes + 107/107 retail coexec + full workspace/GPU suite + architecture gates |
| SF1 bouncyball projectile and pillar effects | sf-strat flat object state + generated shape catalog | GSTRATS.ASM `fire_plasma`/`fire_beamball`/`fire_shortplasma`/`fire_Hplasma`; DSTRATS.ASM `pillar3fall_i`; KSTRATS.ASM `pillar3ffall_i`; USHAPES.ASM `bouncyball` | FIXED (all four weapon constructors and both pillar impacts allocate the authored mesh through one semantic constant instead of a null shape; both pillar variants preserve their distinct source offsets and dead explosion-callback object; the fog-pillar initializer executes its first strategy body immediately) | exact shape/position/callback/kill-state/timing/weapon regressions + complete strategy package + all three unattended routes + 107/107 retail coexec + full workspace/app/GPU suite + default build + architecture gates |
| SF1 flat sprite weapon presentation | sf-strat typed `ObjectVisualKind` + sf-render camera-facing scaled sprites | GSTRATS.ASM `fire_plasma`/`fire_beamball`/`fire_reloval`/`fire_relring`/`fire_oval`/`fire_ring`/`fire_shortplasma`/`fire_Hplasma`/`fire_YHplasma`; KSTRATS.ASM `tank1fire` | FIXED (every source `s_sprite_obj` weapon carries the typed scaled-sprite presentation while retaining its authored mesh and weapon statistics; the tank-local H-plasma duplicate now allocates the visible `bouncyball` instead of a hidden null shape; no packed flag or byte-addressed memory is used) | exact mesh/statistics/visibility/presentation regressions across 129 focused tests + complete strategy package + all three unattended routes + 107/107 retail coexec + full workspace/app/GPU suite + default build + architecture gates |
| SF1 bazooka destruction debris | sf-strat flat object fields + generated shape catalog | GA2STRAT.ASM `bazooka1L_Istrat`/`bazooka1R_Istrat`/`bazookaL_Istrat`/`bazookaR_Istrat`/`bazexp_Istrat`; PSHAPES.ASM `bazz_1p`/`bazz_1q`/`bazooka1`/`bazooka2` | FIXED (both bazooka families retain named body-debris and detachable-barrel shapes in ordinary object fields; destruction emits the two authored body pieces and a pose-matched barrel with the exact 30-tick fall lifecycle instead of a null mesh) | exact initializer/debris-count/shape/pose/rotation/countdown/collision regressions + deterministic 473-shape generation + complete strategy package + all three unattended routes + 107/107 retail coexec + full workspace/app/GPU suite + default build + architecture gates |
| SF1 walking-mech topple effects | sf-strat flat object state + generated shape catalog | DSTRATS.ASM `walking_right`/`walking_left`/`fallover_strat`; SHAPES.ASM `walker_r`/`walker_l`; USHAPES.ASM `explosion` | FIXED (each leg failure selects its distinct authored toppled body; the right-fall and final body effects use the visible small-explosion mesh while the left-fall effect deliberately retains retail nullshape; wobble/fall timing and rotated offsets remain unchanged) | exact left/right body geometry and stable-ID parity + direction/asymmetry/final-impact regressions + deterministic 473-shape generation + complete strategy package + all three unattended routes + 107/107 retail coexec + full workspace/app/GPU suite + default build + architecture gates |
| SF1 opening player strategies | sf-map typed opening catalog + sf-strat player initializers | BGS.ASM `pstrat`; PISTRATS/PSTRATS/GCSTRATS | FIXED (all 27 spawned maps and shell presentations dispatch their authored opening initializer; ground-dive creates the linked duplicate ship/camera graph; underground, colony, and nucleus handoffs install live/collision/death behavior) | exhaustive source-declaration and initializer-state tests + focused fly-in/camera tests + all three unattended routes + retail coexec + architecture gate |
| Map scroll/advance (lastplayz/mapcnt) | sf-game game.rs:244 | WORLD.ASM:50-90 | VERIFIED (incl. exitbase lastplayz=0 reset) | freeze-fix agent trace |
| Level bytecode data (30 maps) | sf-map levels/ | LEVEL*.ASM | VERIFIED (byte-identical port) | route1/2/3_parity tests |
| Path system (opcodes, interp, catalog) | sf-path (98 fns) | PATHS.ASM + path data | FIXED (8 of 10: proportional chases, spawn /4*4 Z-X-Y via ROM rotate_8*, ADDW, accel count1, space coupling, ifbetween, childdead, sound2 + set0/ifzero collapses; 8 paths byte-clean vs ROM). Wave-3: gotopos triggers, explode children, linkchild, friend weights, spawnchild /4*8 | audit_path.rs 8/8; path_roffs_rotate8 + pi_tow_0 |
| Enemy strats A (rader/zaco*/houdai/tower/para…) | sf-strat enemy_a | GASTRATS/KSTRATS | FIXED (4 bugs: zacos3, zaco1 homing, houdai aim/gate, zaco3/4 XZ) + inits verified | audit_strats.rs |
| Enemy strats A — remaining tick logic | sf-strat enemy_a | GASTRATS/KSTRATS | FIXED (Highs, Mediums #11–#37, and Minors #1–#14 closed through tick 161) | AUDIT_ENEMY_A_FINDINGS.md + enemy_a_minors_*.rs |
| Enemy strats B (boss7/bossA/bossF families, spacepilon) | sf-strat enemy_b | GB2STRAT/GB3STRAT/DSTRATS | FIXED (all 26 audit findings: playerturn180 port, bossA husk/resurrect machine, inverted gates, 7 notdelay masks, achase toward-zero, live mines, death chains) | AUDIT_ENEMY_B_FINDINGS.md + audit_strats_b.rs |
| Ground strats | sf-strat ground | GSTRATS | VERIFIED (stayrel/gnd/stayrelhard180yr exact; staydist fixed to per-tick tracking) | audit wave 3 |
| Boss inits (boss2/bossg/boss8) | sf-strat bosses | GBSTRATS/D2STRATS/GB3STRAT | VERIFIED | audit_boss agent line-diff |
| Boss tick state machines | sf-strat bosses/enemy_b | *STRATS | FIXED (boss2/bossg/boss8/seamon/boss1 audits closed; kami→hmissile3) | AUDIT_BOSS_TICKS_FINDINGS.md / AUDIT_BOSS_TICKS2_FINDINGS.md |
| SF1 Route 1 Andross encounter | sf-strat flat object state + sf-render generated mesh catalog | GB3STRAT.ASM:1252-3140; SHAPES2/SHAPES4 `boss_b_*` | FIXED (both face and walking forms retain their authored attack/death flow; exact active-bit animation timing, yaw-only jumps, fall/bounce integration, split-part links and parent shutdown, collision-zone reactions, pounce/random-position countdowns, damage smoke, kick body, and detached foot replace the former scoped animation and mesh substitutions; shipping state remains typed and flat) | line-by-line retail-source audit + focused encounter regressions + deterministic 473-shape generation and exact mesh parity + complete sf-strat package + all three unattended routes + 107/107 retail coexec + full workspace/GPU suite + architecture gates |
| SF1 Macbeth bossH encounter | sf-strat typed mother/leg modes and ordinary object graph | D3STRATS.ASM:34-931 | FIXED (removed the condensed timer approximation; restored all 22 mother modes, all 15 leg modes, protected/vulnerable HP phases, child-scale full-transform placement, both impact gates and smoke bursts, teleport lifecycle, top weapon orientation, leg fall/explosion, audio, and boss-bar/death flow) | line-by-line retail-source audit + exact nine-test encounter regression + complete sf-strat package + all three unattended routes + 107/107 retail coexec + full workspace + architecture gates |
| Route/planet progression | sf-game planets/shell | PLANETS.ASM / MAIN.ASM LE_* | FIXED (LE_* warp dispatch + routechange* + level1_5 mapend7; tick 198) | shell warp tests + blackhole.rs + level1_5 mapend |
| Draw/showview (culling, shadow, AF flags, depth) | sf-game draw.rs | MAIN.ASM alienflags_l:2009 + GSU mallrotzsort | FIXED (all 6 + yaw cull `rotate_16xz` mulslog, no float sin/cos) | audit_showview.rs 4/4 + draw::tests yaw (2) |
| Shape colors/palettes/textures | sf-render color_data/shapes/shapes_gl | ShapeHdr, COLTABS.ASM, LIGHT.ASM, DEFSPR.ASM, TEX_01/23.BIN | VERIFIED (54 exact ShapeHdr tables, 55 exact animation records, 98 texture descriptors; packed-nibble GPU texture path; exact source-raster palette-pair dithering and color-zero transparency; dormant COLSMOOTH excluded by ROM build flag) | color_resolution.rs 16 + shape_data_parity.rs 5 + gl_runtime texture/dither readback |
| Shape mesh data | sf-render shape_data + sf-core sf1_shape_metrics | SHAPES/USHAPES/PSHAPES etc. | VERIFIED (473 compiler-generated flat meshes, including all seven runtime explosion shapes, Andross's direct kick/foot meshes, all four hyperspace streak phases, both bazooka body/barrel pairs, and both walking-mech topple bodies; exact animation frames, face topology, authored normals, polygon visibility triangles, unconditional retail Face2 lines, visual extents, and coordinate shifts; no runtime hand-authored overrides or source addresses) | shape_data_parity.rs + sf1_shape_metrics.rs + gl_runtime.rs |
| SF2 polygon textures | sf2-data textures + sf-render shapes_gl | Retail GSU descriptor/layout lookups and source banks 12–14 | VERIFIED (211 exact descriptors, 12 exact coordinate layouts, three 32 KiB packed-nibble banks; all 52 IDs referenced by 126 shapes resolve; no SF1 descriptor reuse or magenta fallback) | sf2-data texture coverage + campaign-missile GPU readback |
| SF2 polygon palettes | sf2-data palettes + typed mission state | Retail five-row BGR555 catalog + live CGRAM phase captures | VERIFIED (all five rows extracted exactly; native renderer selects Standard, EladardSurface, or AstropolisExterior from typed mission phases; Titania and both Carrier stages verified Standard; no source selector or generic memory in shipping state) | color_resolution.rs + sf-app phase-selection test + retail route captures |
| SF2 polygon depth/lighting | sf2-data lighting + sf-render shapes | Retail depth-colour families, light pointer catalog, and shade tables | FIXED (removed SF1-table reuse; generated exact SF2 standard four-bank depth pairs and four-by-ten shade rows; live Eladard, Titania, Carrier, and Astropolis jobs verify Standard depth selection and direct SF2 shade-table reads) | sf2-data extraction 6/6 + color_resolution.rs 16/16 + Mesen depth-color oracle |
| SF2 mission backdrops | sf-render typed frame inputs + native textures | Retail PPU BG1 isolated from live mission savestates | FIXED (removed the opening-sortie backdrop from every mission; exact deep-space, Eladard surface/interior, Titania, and Carrier layers are selected from typed mission phases; Astropolis correctly uses the black PPU layer behind its Super FX scene geometry) | generated FNV tests 5/5 + sf-app exhaustive phase routing + offscreen GPU selection + Mesen mission-background oracle |
| SF2 Eladard mission gameplay | sf2-game typed mission/object state | Natural surface and interior traces plus explicitly classified oracle forcing | FIXED (the prepared-selection retail route naturally clears both surface barriers, activates the first interior switch, crosses both live door transitions, completes the generator objective, and follows the mission-owned escape to the strategic map; shipping has typed access/transit/generator rooms, the exact observed static interior scene, independently timed switch/door state, position-preserving room transitions, the retail 125-to-75 generator lifecycle, both independently destructible pop-up defenders with their recovered motion and aimed laser volleys, exact catalog body bounds, four-shield contact damage, 56-frame contact recovery, and the automatic post-generator transformation/hide/return sequence) | strict resumed retail route + released-shield contact replay + decoded defender/escape behavior + native no-timeout/object-collision/room-state/defender/contact/return regressions |
| SF2 Macbeth mission gameplay | sf2-game typed mission/object state | Retail surface, Knight, shield-controller, core-damage, destruction, and strategic-return traces | FIXED (the Flight-to-Walker first switch exposes two one-hit tower guns; their independent destruction exposes the second switch; the installation appears after 244 frames and finishes descending after 464; Flight entry hands off to the interior Walker, Knight rejects frontal shots but accepts flank/rear attacks, two 100-durability core-room turrets control the eight-frame shield opening, and the 125-durability core completes at 75 before a 588-frame destruction and 528-frame return) | retained retail mission trace + controller-gated core isolation + cleaned reusable Mesen navigation/probe + exact native switch/form/defender/shield/core/return regression + app backdrop/music/bank routing + shipping architecture gates |
| SF2 Battle Carrier mission gameplay | sf2-game typed mission/object state | Natural first- and second-carrier approach, corridor, room-entry, panel-damage, and completion traces | FIXED (exterior admission and corridor arrival require player position; the central rail implements retail cruise/boost, three sentry lifecycles, both shootable controls and blocking passages, both rotating-door pairs, and the retained wall/bulkhead layout; room entry performs the automatic folding sequence and instantiates the central core, rotating overhead coupling, two decorative broken panels, and two intact armor panels; the visible `CC40` panels reject direct shots, while independently staggered invisible `BECC` windows use their retail 16-unit bounds and relay two integrity per hit without taking damage themselves; each linked panel alone becomes `CC5C` at 90 and both are required for completion) | retained full-route and resumed-room traces + native rail-speed, sentry lifecycle, gate-control, rotating-door, former-timeout stall, proxy-window timing/lifetime/damage, transformation, and two-carrier route regressions |
| HUD/score/lives logic | sf-game/sf-render | MAIN/SPRITES.ASM | FIXED (Critical #1–#3 score/lives/specials + High #4 bosshp; Medium shield/talk/bomb FIXED) | AUDIT_HUD_SFX_FINDINGS.md |
| SF1 end-level tally | sf-game typed tally state + sf-render UI/HUD | MAIN.ASM `end_level_seq`; MTXTPRT `mshowpercgraph`/`mshowteammate`; FACE.CGX | FIXED (retail count-by-three, delayed score commit, threshold/bonus delay and credit award; exact graph, text positions, teammate portraits/dead frames, shield bars, retained stage scene and flight HUD; no generic memory or machine state in shipping code) | retail Mesen semantic/frame oracle + shell phase tests + exact FACE atlas hash + offscreen framebuffer check |
| SFX trigger mapping (play_se ids) | sf-strat/sf-audio | SOUND.ASM | FIXED (F1–F6 + hitwall/laser/missile/battry/ring/movewall/door/sea; separatemissile_l dead ROM helper) | AUDIT_SOUND_IDS_FINDINGS.md |
| SPC engine | sf-spc | SPC700 program | VERIFIED (plays correctly; bit-exactness N/A scope) | |
| Frame scaling | sf-game | framescale | VERIFIED | framescale.rs |

## Wave log
- 2026-07-22: restored the typed sprite presentation for SF1's complete plasma, beamball, oval, ring, short-plasma, H-plasma, and Y-homing weapon family. Every constructor whose source ends in `s_sprite_obj` now marks the ordinary object as a camera-facing `ScaledSprite` while preserving its authored mesh, motion, collision, and damage fields. The tank-specific H-plasma duplicate now allocates the source `bouncyball` and remains visible instead of spawning a hidden null shape. No packed display flag, source address, or byte-addressed memory enters shipping state. Exact mesh, statistics, visibility, and typed-presentation assertions pass across 129 focused tests together with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including app/SDL and real GPU readbacks, the default build, and both flat-state architecture gates.
- 2026-07-22: restored the complete SF1 walking-mech topple presentation from `DSTRATS.ASM`. Breaking the left leg now selects `walker_r`; breaking the right leg selects the newly generated `walker_l`; both retain the authored local body shift, wobble, and fall timing. The right-fall flash and final body effect allocate the source `explosion` mesh, while the left-fall flash deliberately keeps `nullshape` as the retail source specifies rather than applying a symmetric approximation. Deterministic generation now covers 473 flat meshes, with exact 17-vertex/21-face and 18-vertex/23-face pose regressions. Direction, effect-asymmetry, final-impact, stable-ID, and geometry tests pass together with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including app/SDL and real GPU readbacks, the default build, and both flat-state architecture gates.
- 2026-07-22: restored SF1 bazooka destruction to the retail two-part visual chain. Both the `bazz_1` strategy pair and the map-used bazooka pair now seed named body-debris and barrel shapes in flat object fields. `bazexp_Istrat` detaches the source barrel mesh at the destroyed object's exact position and orientation, disables its collision, and runs the authored 30-tick falling lifecycle; the following generic explosion emits both stored body-debris pieces. The compiler now generates `bazz_1p`, `bazz_1q`, `bazooka1`, and `bazooka2` directly from PSHAPES, bringing deterministic coverage to 472 flat meshes. Exact initializer, body-piece, barrel-shape, pose, rotation, countdown, and collision regressions pass together with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including app/SDL and real GPU readbacks, the default build, and both flat-state architecture gates.
- 2026-07-22: removed the remaining null-shape substitutions from SF1's bouncyball family. The four plasma/beamball constructors and both pillar impact flashes now select the existing authored `bouncyball` mesh through one named flat shape constant. The two pillar variants retain their distinct source Z placement, install the generic explosion behavior in all three callback roles, and create an already-killed impact object for that lifecycle; the fog-pillar initializer also falls through into its first strategy body on the creation frame as the assembly does. Exact shape, coordinate, callback, kill-state, weapon-stat, and same-tick regressions pass together with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including app/SDL and real GPU readbacks, the default build, and both flat-state architecture gates.
- 2026-07-22: recertified SF1 hyperspace emission against `PISTRATS.ASM` and the retail line renderer. Both emitter initializers now fall through into their first authored tick instead of waiting a frame. Each streak consumes the exact low/high X, low/high Y, and roll random draws; uses independent signed screen-space coordinates rather than inheriting the emitter's X/Y; applies the roll-only 50-unit offset and `Space_ViewCY`; retains the emitter depth until `hyper_Istrat` runs; and decrements Z by 80 on later ticks. The outgoing 64-tick sequence now selects `hyper`, `hyper2`, `hyper3`, and `hyper4` at the source table boundaries. The shape compiler emits the three missing meshes and encodes MOBJ's rule that Face2 lines bypass polygon visibility, including `hyper4`'s intentionally unusable shared visibility indices. Deterministic generation now covers 468 flat meshes. Exact initializer, random-stream, coordinate, countdown, phase, mesh, and line-semantics regressions pass with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including app/SDL and GPU readbacks, the default build, and both flat-state architecture gates.
- 2026-07-22: completed the retail-source recertification of SF1 Route 1 Andross. The walking form now uses the actual `boss_b_0`, `boss_b_6`, and `boss_b_7` meshes, bringing deterministic compiler coverage to 468 flat shapes. Both forms use the source active-bit animation rules; jumps generate yaw-only horizontal motion; falling bodies apply gravity, bounce, and position exactly once; landing states no longer invent coordinate snaps; and decrement/selection gates use their authored tick. Split parts now carry an ordinary one-based object link instead of corrupting the mother's coordinate fields through an unrelated family-tree helper, honor parent shutdown, defer initializers through the normal scheduler, and resume correctly after collisions. The walking body restores left/right/top reaction zones, contact explosions, sound/flash behavior, pounce exhaustion, foot trails, and the under-60 damage-smoke cadence. Focused encounter and exact-mesh regressions pass together with the complete strategy package, all three unattended routes, 107/107 retail coexecution cases, the full workspace including real GPU readbacks, deterministic code generation, and both flat-state architecture gates.
- 2026-07-22: replaced the condensed timer-driven SF1 Macbeth spider boss with the complete authored encounter from `D3STRATS.ASM`. Flat typed enums now drive all 22 mother phases and all 15 leg phases, including both three-raised-leg gates, full walk/scuttle/oscillation/flight choreography, protected and red vulnerable leg windows, both ground impacts and smoke bursts, teleport creation/retraction, and the exact attack-loop return. The ordinary linked object graph retains the five legs, spinning top, and teleporter; child placement now applies the source child scale and the mother's full orientation, while the plasma muzzle preserves the top's current pitch and yaw. Leg destruction detaches into its falling arc before the generic explosion lifecycle, and the body bar and vulnerability remain coupled to the surviving legs and impact count. Exact initialization, transform, phase, weapon, collision, audio, HP, death, and full-loop regressions pass together with the complete strategy package, all three unattended routes, 107/107 retail coexecution tests, the full workspace including real GPU tests, the default build, and both flat-state architecture gates.
- 2026-07-22: restored the complete generic SF1 `explode_Istrat` lifecycle instead of deleting every in-view object immediately. The shape compiler now emits all seven runtime explosion meshes plus a flat semantic extent/coordinate-shift profile for every one of the 468 renderable shapes. Strategy code uses typed size classes to reproduce the 64/128/256 boundaries, source signed sprite adjustment, 4/6/8-frame sprite selection, 12-frame debris lifetime, oversized half-rate path, two random rotations, attached-fire removal, range-selected sound, special scoring, and no-polygon gate. A typed `ObjectVisualKind` replaces the packed scaled-sprite flag; rendering billboards that mesh by canceling the camera basis and applies the source size in ordinary model space. Restoring the sprite allocation and random draws intentionally changed downstream object slots and poses, so the affected boss/enemy regression fixtures were corrected only from their first EXPSTRAT-backed boundary. Exact four-class, lifecycle, no-polygon, player-death, full strategy-package, 468-shape coverage, rotated-camera, real GPU readback, player-trace, all three unattended routes, 107/107 SF1 retail coexecution cases, the complete workspace suite, and both flat-state architecture tests pass.
- 2026-07-22: restored SF1's terminal `pexplode_Istrat` object graph, red-circle transition, and fade timing. The dying ship now hands authoritative player, camera-target, and circle ownership to a flat inert anchor positioned one velocity step ahead; zeroes its own motion; removes its shadow; installs the generic explosion callback in all three semantic lanes; and creates the separate large-particle explosion at the original position. The port retains the source's dying flag while setting dead, decrements one life, plays the terminal cue once, and owns the exact TRANS.ASM radius, deceleration-to-zero, settled 171 radius, and five-bit red progression as typed domain state. A dedicated additive GPU pass draws the 64-segment circle in source coordinates, then the shell waits the authored 20 ticks and completes its 30-step native black fade before continue dispatch. The frozen C fixture's final six records were corrected only at this source-backed boundary because that older implementation omitted the anchor and particle and retained the ship. Exact lifecycle, first-frame, next-tick, fade-boundary, typed-state, trace-parity, and real GPU readback regressions pass together with the entire strategy package, all three unattended routes, 107/107 SF1 retail coexecution tests, the complete current SF2 native package, 29 SF2 oracle-runtime tests, and both flat-state architecture gates.
- 2026-07-22: restored SF1's coupled cockpit-death transition from `PSTRATS.ASM`. A lethal hit inside the cockpit now disables the ship, detaches all three damage proxies, latches the one-shot death audio, and executes the source's immediate first ejection body plus the remaining 22 countdown bodies without setting the shell's dying flag early. The transition flies straight, adds the authored death roll, creates the cockpit and rear Arwing props at count 19, applies the semantic space bounds without clearing sequence state, then re-enters the ordinary crash initializer. That handoff installs the dedicated hit-flash and terminal-explosion callbacks, clears collision-disable, starts the 60-count crash body on the lethal frame, and decrements lives exactly once at the terminal explosion. This corrects the frozen C trace, which deferred the first crash body and retained collision-disable; the deterministic fixture now records the source-backed divergence. Normal cockpit exit likewise preserves scripted control locks and restores interactive control only outside a sequence; common space setup no longer clears the source-untouched shadow flag. Exact transition, callback, audio, collision, and terminal-state regressions pass together with the entire strategy package, all three unattended routes, 107/107 SF1 retail coexecution tests, 108/108 SF2 native tests, 29 SF2 oracle-runtime tests, the complete workspace, real GPU readback, and both flat-state architecture gates.
- 2026-07-22: replaced the generic SF1 player spawn fallback with the exact opening strategy declared by every spawned `BGS.ASM` background. A typed catalog covers all 27 route, special, training, continue, title, intro, and credits entries and dispatches hangar launch, interior/space/planet/colony fly-ins, hyperspace exit, ground dive, steady planet/underground flight, long-tunnel exit, and the two presentation strategies without carrying source pointers into shipping state. Enabling the real Route 1 dive exposed and fixed the missing linked duplicate-ship/camera graph; enabling Route 2's colony fly-in exposed and fixed the omitted live, collision, and death handoff, with the same source omission closed for underground and nucleus flight. Exhaustive source-boundary and initializer-state tests pass together with all three unattended routes, 107/107 SF1 retail coexecution tests, 108/108 SF2 native tests, real GPU readback, the complete workspace suite, and both shipping architecture gates.
- 2026-07-22: restored SF1's missing player-view controls and the view transitions previously represented only as numeric state changes. The port now owns typed exterior, close-exterior, entering-cockpit, cockpit, and leaving-cockpit modes plus typed two-view and cockpit-capable cycles. Select changes view only on the retail input edge and behind the exact distance, black-hold, control, sequence, death, boost/brake, and no-view-change gates, with the authored sound cue; even the cleared pre-background option bound retains the source's wrap-and-sound result instead of inventing another gate. Every playable opening and each runtime background declaration is mapped from BGS.ASM; parameterless declarations intentionally preserve prior state. Warp exit, inside-space fly-in, both carrier approaches, the colony approach, MAP3_6, and PLAYER_OUTVIEW now invoke the real registered cockpit transition through named callback fields. Source-boundary, gate, transition, scripted-exit, and trace-parity tests pass together with the full workspace, all three unattended routes, 107/107 SF1 retail coexecution tests, 108/108 SF2 native tests, GPU readback, and both shipping architecture gates.
- 2026-07-22: restored SF1's missing opening screen wipes without carrying the source `circletab` interpreter into shipping code. The map catalog now records every route map's `mstarwipe`, `mscramwipe`, or no-wipe operand directly from `MAPS/*.ASM`, including Corneria's separate initializer star and later launch-corridor band reveal. Flat typed state owns only the semantic kind, authored frame, active flag, and short black hold. The star aperture walks the exact eight `MDATA.MC` line records with the integer major-axis algorithm used by `MBUMWIPE.MC`; the horizontal aperture expands eight source scanlines per side. The renderer scales those 224-by-192 scanline intervals over the HD output and draws only the opaque outside mask. Source-boundary tests cover all 19 route maps plus Training, real GPU readback proves the fully closed frame, exact 96-row midpoint, and retained final star corners, pause remains locked only while the transition is active, all three routes finish unattended, 107/107 retail coexecution tests pass, and both flat-state architecture gates remain green.
- 2026-07-22: replaced Macbeth's missing strategic destination with one native typed mission. Retail traces recovered both Walker switches, the two tower guns, the delayed installation descent, Flight-only entrance, Knight's frontal shield, the two core-room turrets, controller-owned shield opening, 125-to-75 core threshold, and the exact destruction/return cadence. Shipping models those concepts as flat Rust fields and enums, decoded catalog shapes, named constants, and ordinary object collisions; source object addresses and raw state remain confined to the cleaned Mesen oracle. The focused end-to-end regression completes the visit only through its real objectives and proves campaign rescue, while the full game/app suite, unattended route simulator, smoke renderer, audio routing, and both shipping architecture gates pass.
- 2026-07-23: closed the last explicit Eladard gameplay gap with a released-shield A/B oracle replay. The same resumed interior state was held first at a defender and then at the midpoint between both defenders. Only direct overlap set the player collision response; releasing the verification-only shield restoration immediately afterward proved that each contact removes exactly four shield, with the next accepted contact exactly 56 retail frames later. The host collision routine independently confirms that damage comes from the defender's decoded four-valued collision field, while catalog shapes lock the strict Walker-versus-defender overlap limits at 48/60/65 accepted and 49/61/66 rejected world units. Shipping assigns the semantic contact damage to the typed defender, applies it only to the live Walker through ordinary object bounds, preserves the zero-shield survival rule, and uses the observed contact-specific recovery without source addresses or generic memory.
- 2026-07-22: recovered both genuine Eladard interior defenders and excluded two previously suspected actors after their object lifecycles proved unrelated. Each defender is now a mission-owned, one-health typed enemy at its observed world position. Its flat object behavior executes the retail 14-delta rise/fall motion at the six-frame cooperative cadence, continuously chases the player's yaw, fires immediately before the seventh displacement, returns to its initial height, and begins the next cycle at the observed 252-frame boundary. The shared hostile-projectile allocator supplies the semantic sound cue and ordinary enemy-laser fields; a controlled retail player pose reproduces both captured launch poses, pitch/yaw pairs, velocities, attack power, and lifetime exactly. Either defender can be destroyed independently with one ordinary shot, while the access switch and both doors remain progression-driven and do not wait for the hazards. The focused oracle regression, all 93 `sf2-game` tests, and both shipping architecture gates pass with only flat typed domain state in the port; body-contact behavior and an unforced retail return remain explicit Eladard follow-up work.
- 2026-07-22: closed Eladard's fabricated manual escape gate with the retained unforced core run. The oracle configuration used no teleport, target binding, health/objective forcing, collision forcing, or mission completion; shield preservation was the only route-isolation aid. Ordinary fire changed both objective mirrors from one to zero at frame 44,656. Retail then owned the entire escape: Walker-side folding began 56 frames later, flight-side folding at 100, stable flight at 140, the player became the decoded no-shape object at 204, and the game returned to strategic mode 1,337 frames after the objective change. Shipping now derives that sequence from the flat typed Eladard phase, disables control and collision during the cutscene, and returns without Select or any other player input. The four-frame native cadence reaches the nearest representable return boundary; the full typed route regression locks every observed presentation boundary and proves that input cannot gate completion. Defender body-contact behavior remains the only explicit Eladard gameplay gap.
- 2026-07-22: removed the fabricated elapsed-time Battle Carrier assault. Waiting beyond the former 9,822-frame return boundary no longer advances or destroys anything. The craft must reach the exterior admission point and then the reactor end of the central rail; the automatic room approach continues to the observed folding position, opens the room 40 retail frames into the existing 56-frame transformation, and returns control only in stable Walker form. The room now allocates the exact catalog meshes used by retail: both `CC40` panels begin at 100 integrity, every effective projectile collision removes two regardless of player weapon strength, and reaching 90 changes only that target to `CC5C`. Completion and the relative return sequence begin only after both typed panel objects break. Retained first- and second-carrier traces establish the common entry and transformation, while the resumed first-carrier room trace locks the 100-to-90 panel lifecycle and 1,164-frame post-objective return. The invisible rotating `BECC` collision proxy and the corridor's remaining doors, speed boost, and defenders are still explicitly partial.
- 2026-07-23: recovered the Battle Carrier's central rail and sentries from the natural first-carrier corridor trace and the longer second-carrier route. Shipping now fixes the craft to the retail centerline and forward bearing, approaches the observed neutral speed of 30, and responds to held boost by approaching 40. Three distinct typed sentry slots activate 2,048 world units before their positions at depths 5,520, 8,704, and 10,240. Each one-hit `EE0C` actor traverses 256 lateral units in 16 steps, tracks the player, fires the observed three-shot enemy-laser volley, and withdraws if the player does not destroy it. Their behavior and projectile lifetime are flat semantic state; no object bytes, source strategy pointers, or segmented-memory model enter the port. The two shootable door/controller sequences and reactor `BECC` proxy remain explicitly partial.
- 2026-07-23: recovered both Battle Carrier gate-control sequences and the intervening rotating doors. The first controller at depth 7,028 changes through the decoded active/transition/settled meshes `D1D4 -> D1B8 -> F234`; the reactor controller at 12,148 independently follows `E974 -> E958 -> D5C4`. Ordinary player projectiles consume against either 100-integrity controller, award the one point visible in the retail corridor score, and start a typed opening state; the player remains clamped 256 units before the corresponding bulkhead until the 96-frame passable boundary rather than flying through closed scenery. The two `D308` pairs at depths 8,192 and 9,728 begin from the captured player-approach positions and rotate their port leaves to the observed quarter turn in 52 frames. Shipping also instantiates the retained 23-object corridor wall, side-door, and nested bulkhead layout at the trace coordinates. The controls continue their 320- and 292-frame mesh sequences through reactor approach just as the retail objects overlap room loading. A focused regression proves that waiting cannot open either gate, both shots are independently required, both automatic door pairs settle correctly, and the reactor path remains reachable. All state is semantic fields and stable object IDs; only the invisible `BECC` reactor proxy remains open for this mission.
- 2026-07-23: recovered the Battle Carrier reactor's real damage boundary. Decoded retail panel logic increments an independent phase counter, checks the player's bearing in the authored wide yaw arc, and creates a short-lived `BECC` child; the child opens after 32 retail frames, carries the catalog's invisible 16-by-16-by-16 collision bounds, and expires before the next 192-frame opportunity. Shipping represents those concepts as typed `Waiting`, `Opening`, `Exposed`, and `Destroyed` states plus stable object IDs. The retained first- and second-carrier traces lock the initial starboard/port stagger at 16/172 retail frames for this campaign path, the proxy coordinates `(1,630,-216,13,312)` and `(928,-216,13,312)`, and the fixed two-integrity relay. Direct shots against visible `CC40` geometry do nothing; proxy hits consume the projectile, leave proxy integrity at 100, damage only the linked panel, and cancel only that panel's window at the 90-integrity `CC5C` transition. The room now also includes the missing rotating overhead `CC08` coupling and two pre-broken decorative `CC5C` panels. A focused regression covers direct-shot rejection, exact initial placement and stagger, opening, expiry, recurrence, independent panel damage, and completion; both unattended campaign routes pass through the recovered windows.
- 2026-07-22: removed the fabricated elapsed-time Eladard completion path. Shipping no longer auto-clears the surface, invents a Wall Spider, presses an assumed platform switch, drains generator durability, or forces either craft transformation. The two observed surface defenses and the interior generator frame/core now use semantic decoded shapes, stable object IDs, typed durability states, ordinary world coordinates, and the shared weapon-collision/destruction lifecycle. Base entry requires the player to clear both defenses, reach the central entrance in Walker form, traverse the interior passage, destroy the observed 125-durability core, survive the destruction interval, and manually return to flight form. The route regression advances past the former automatic return without changing an objective, then completes every gate through collision and player-form state. The compact campaign trace is explicitly treated as prepared-state mission-identity evidence, not proof of natural Eladard mechanics; room geometry, defenders, and an unforced retail return remain open.
- 2026-07-22: recovered the four planetary missions previously absent from the native SF2 route as distinct retail entries rather than Titania/Eladard aliases. Independent saved-map replays prove Venom, Macbeth, Meteor, and Fortuna enter separate setup maps, active maps, player spawns, and audio programs; together with Titania and Eladard, the retail planetary audio sequence is `$062/$076/$08A/$09E/$0B2/$0C3`. The offline renderer and ordinary PCM player now expose all six worlds as semantic cues and sound banks. Four 180-second music files and 792 pilot/bank sound files passed response thresholds, and an independent full regeneration matched all 796 files byte-for-byte (aggregate manifest SHA-256 `1d1426a46155feed0b5f64db3c687749da744eb926c0e2d7abff569ade794c99`). Source addresses and program records remain confined to oracle tooling; the shipping surface contains only named enums and files.
- 2026-07-22: removed the final SF2 path-classification CPU escape. A deterministic extractor now emits the complete compound collision catalog: 98 ShapeHdr mappings share 78 profiles with 355 logical groups, 407 animation variants, and 273 convex polygon footprints; the remaining 479 shapes use their exact header bounds. Typed Rust performs object filtering, animation selection, broad and precise footprint tests, nearest-surface selection, collision flags, link fallback, and three-way contact classification. Retail fixed-point rotation, polygon, and plane kernels remain confined to the feature-gated oracle compatibility host; shipping SF2 stays flat typed and processor-free. The differential suite accepts and compares every compound record, all simple X/Y/Z edges across the four reachable initializer shapes, four rotated polygon orientations, and all three non-solid fallback classes.
- 2026-07-23: restored the complete SF2 second-sortie mission-radio presentation. A deterministic retail save-state replay isolates the semantic Slippy guidance request, all opening and closing boundaries, the still-open close cue, and every one of the 39 native portrait poses. Shipping state is an ordinary mission-owned struct containing a message enum, presentation phase, elapsed time, and talking pose; the renderer consumes exact generated Slippy/interference artwork plus the localized retail panel, while audio consumes two named PCM effects. Fresh code generation matches after canonical formatting, the real GPU output is checksum-locked and visually inspected against the source capture, all 96 mission-bank/pilot cue variants regenerated byte-for-byte, and a hidden eight-tick app launch validated the complete runtime asset catalog. The default shipping architecture gate passes with all source-machine inspection confined to the oracle tracer.
- 2026-07-23: restored SF2 capital-craft positional engine audio. The nearest active large craft now owns typed source identity, four distance bands, and three stereo positions; the app routes that state to an independent positional mixer layer and restarts it only when the source craft changes. All mission-bank and pilot variants are named PCM assets, and runtime validation requires the complete matrix. Oracle correlation, native nearest-source regressions, mixer-layer isolation tests, real startup, and the shipping architecture gate pass.
- 2026-07-23: recovered Eladard's retail objective route from a prepared mission-selection state without forcing objective counts, target health, collisions, teleports, or mission completion. Ordinary player fire advances the two `D74C` surface barriers and the objective count from three to one; the base handshake then reaches the first interior gate. The nearby `EF5C` switch runs its own pressed path and becomes `EF78`, both `EC14` doors become `EC30`, and live player movement advances the map gates `33EC -> 33F0 -> 3490 -> 3494 -> 34DF`. In the final room, `E958/E974` is an animation/controller actor rather than the damage objective; the already-present `EB6C` core starts at 125, loses two per ordinary hit, and completes the objective exactly at 75. Shipping now preserves that typed initial health and threshold instead of draining to zero. The replay uses shield preservation only to isolate the long route, and all 92 `sf2-game` tests plus both flat-state architecture gates pass. The following typed-room tranche addresses the captured static geometry; defenders remain explicitly partial.
- 2026-07-23: replaced Eladard's synthetic one-corridor threshold and generator-room teleport with flat typed room state. Entering the base now allocates the observed 21-piece access/transit scene at retail world coordinates, plus a live pressure switch and two separately owned doors. Contact changes the switch mesh, starts the observed 120-retail-frame first-door delay, and permits the first room crossing only after that door is open. The second door starts its own 172-frame approach sequence; crossing it at the captured retail position preserves player motion and enters the generator chamber instead of relocating the craft. Both doors use semantic closed/open meshes and state enums, and teardown follows typed object ownership. The full 92-test native suite and both shipping architecture gates pass without source addresses, processor names, segmented memory, or a generic memory field. Interior defenders remain the next explicit Eladard gap.
- 2026-07-22: restored SF2 hostile-laser audio at the projectile-allocation boundary. An optional object snapshot in the read-only Mesen audio tracer ties the effect command to a newly active retail hostile-laser object rather than to player charge or an unrelated world event. Every shipping hostile-projectile family now reaches one shared flat typed allocator, which queues `HostileLaser` only after object allocation succeeds; an exact opening-sortie regression proves both the first cue and the retail boundary where two hostile shots begin together. The four-slot semantic event field also preserves a same-tick player shot and mission-radio cue. The offline renderer generated all 48 mission-bank/pilot variants above threshold (42 distinct PCM hashes), and an independent regeneration matched every file byte-for-byte. Runtime asset validation now requires the expanded semantic effect matrix plus the dedicated engine, ambience, and positional layers. Source commands, addresses, machine state, and object-list inspection remain confined to oracle tools; other unclassified hostile loops remain separate accuracy surfaces.
- 2026-07-22: restored SF2's player combat-audio path without putting the source sound machine into shipping code. A read-only Mesen trace correlates the flight engine, rapid-shot pair, charge-building transition, charge-ready transition, and charged-release pair with native gameplay boundaries and exact source timing. Auditing all eight mission programs and all six selectable pilot data sets proved that the rendered PCM varies across both axes, so the app now carries typed mission-bank and active-pilot selections into ordinary named WAV playback. Flat `AudioState` fields expose only semantic weapon events and charge state; source commands, program records, and hardware-port observation remain in offline oracle tools. The renderer emits 240 threshold-checked WAVs plus 48 engine loop points, and an independent full regeneration matched all 288 file hashes. Asset validation requires the entire matrix, exhaustive tests cover all 16 mission visits and six pilots, both shipping architecture gates pass, and a hidden app launch reached the opening mission with the new catalog. Dynamic hostile/positional sound remains a separate open accuracy surface.
- 2026-07-22: restored SF2's soundtrack routing across the full typed campaign surface. A read-only audit of 10,038 retained retail WRAM captures identifies the live audio program for pilot selection, ordinary space combat, the three-fighter interception, Titania, Eladard, both Battle Carriers, Mirage Dragon, the rival/pressure encounters, Astropolis, Game Over/Results, and the staff roll. The app now selects semantic cues from `GameMode` and `MissionVisit`; shipping audio only consumes ordinary named PCM files and contains no source addresses, registers, segmented memory, or generic byte RAM. All nine new 180-second assets rendered above the silence threshold and regenerated byte-for-byte, every mission visit has an explicit routing regression, and a real hidden SF2 startup validated the complete asset catalog.
- 2026-07-22: restored SF2's player-damage and Game Over/Continue loop. Deterministic retail probes isolate the genuine hostile projectile and prove its asymmetric player-relative hit box (`x -76..58`, `y -49..117`, `z -75..53`), attack power 2, non-consumption on impact, zero-shield survival rule, 40-frame recovery, 280-frame destruction, 400-frame Game Over prompt, and 172-frame accepted return. Shipping uses flat typed damage/game-over enums and ordinary object fields; it promotes the surviving wingmate without fabricating a reserve, retains that craft's 40 shield, and retries the current campaign route. The app now carries the typed phase and choice into a visible Game Over/Continue screen, autoplay accepts the reserve handoff, diagnostics have a stable mode, and audio restarts the dedicated oracle-rendered semantic music program. Exact six-face collision tests, end-to-end promotion, generator/Lua checks, both architecture gates, offscreen GPU integration, and the complete workspace suite pass. The GPU test binary is serialized internally after exposing an unsafe parallel-backend teardown race.
- 2026-07-22: corrected projectile ownership in SF2's accepted Leon duel. All six tracks previously labeled as enemy lasers are the combat-autopilot player's shots: their raw object records use the retail player-projectile shapes and player health, attack, and collision policy, while the actual hostile projectile shape `E3A8` never appears before Leon's accepted frame-664 defeat. A dedicated Leon importer now filters only true hostile shapes and reproducibly emits zero hostile lifetimes for this route. Shipping no longer allocates scripted Leon lasers, and an every-tick native regression proves that no enemy projectile is injected through the complete sortie. The player weapon system remains the sole owner of the recorded shots; the architecture gate continues to pass with flat typed state and no generic memory or source-machine vocabulary.
- 2026-07-22: removed all six sampled hostile-projectile tracks from SF2's two final-rival encounters. Shipping now executes the shared flat typed projectile dynamics against live current and previous player positions: 151 semantic actions cover the final pursuer's three lifetimes and 197 cover Wolf's three lifetimes. A deterministic save-state replay reproduced the accepted full encounter records, while reproducible compact imports independently replay all 49 final-pursuer and 89 Wolf projectile poses before generation. Exhaustive native regressions prove every pose, velocity, behavior, weapon, typed phase, lifetime, and cleanup boundary. The old tracks compile only as test evidence; all related generator checks, the 70-test sf2-game suite, and both architecture gates pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed the last two sampled final-campaign rival-pose tracks from SF2 shipping. The final pursuer and Wolf blockade now share flat typed phase, target-speed, acceleration, and movement-progress state; 328 and 355 semantic actions execute their distinct retail entrance, approach, maneuver, player-facing attack, altitude limiting, launch, and departure schedules. Replaying the accepted campaign save state reproduced the full 4100-frame source segment byte-for-byte, and a deterministic importer resolves operations that straddle presentation boundaries from independent pre/post-operation angles. Exhaustive native regressions match every one of the 209 final-pursuer and 224 Wolf retained poses, the final-pursuer hidden frame, and both departure boundaries. Sampled rival poses compile only as test evidence; generator checks and the native architecture gate pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed the sampled rival-pose track from SF2's Leon duel. Leon now owns flat typed maneuver and movement phases, speed target, acceleration, and motion progress; 119 semantic actions execute the retail approach, combat-altitude hold, roll leveling, movement, and the one-boundary prepared-movement continuation. The isolated non-forced operation trace matches every accepted rival pose through frame 660, and its raw SHA-256 source reproduces the compact fixture and generated Rust byte-for-byte. The accepted defeated-rival trace supplies departure at frame 664. An exhaustive native regression matches all 153 retained poses, departure, score, and completion accounting; the old pose track compiles only as test evidence. Leon and Pigma generator checks, the shipping architecture gate, unattended route simulation, renderer/runtime suites, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed the sampled rival-pose track from SF2's Pigma duel. Pigma now owns flat typed maneuver phase, speed target, acceleration, wave progress, escape progress, and player-altitude history; semantic actions execute the retail approach, combat altitude hold, live player-facing attack, authored second pass, deceleration, and escape. The raw save-state operation trace reproduces the compact fixture and generated Rust byte-for-byte, while the exhaustive native regression matches all 298 retained poses and the departure boundary. The old pose track compiles only as test evidence. Both generator checks, the shipping architecture gate, renderer smoke path, unattended route simulations, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed all three sampled hostile-projectile tracks from SF2's Pigma duel. Shipping now executes the shared flat typed projectile dynamics against live player positions; a typed pending-contraction phase preserves the retail cooperative yield where the X component is committed one presentation boundary before Y and Z. A resumed save-state trace reproduces the accepted 420-sample duel and exact 1677/1679 return boundaries, while the compact semantic fixture independently replays all 120 retained projectile poses before generation. The old pose tracks compile only as test evidence, and the exhaustive native regression proves every pose, velocity, behavior, typed phase, lifetime, and cleanup boundary. All generator freshness checks, the default build, all 64 default-feature sf2-game tests, the shipping architecture gate, renderer smoke tests, unattended route simulations, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed all three sampled target-pose tracks from SF2's campaign-missile interception. Each missile now owns a flat typed steering state and advances through semantic initialization, one-unit pitch/yaw adjustments, two-unit spin, exact signed-logarithmic flight, cooperative movement continuations, visibility, and departure actions. The resumed retail operation capture matches the accepted sortie trace exactly across every shared camera, player, wingmate, and complete-object sample. Its compact semantic fixture independently replays all 1817 retained missile poses before generation; the old tracks compile only as test evidence, and the native regression compares every visible pose plus all departures. Generator freshness checks, the default build, all 63 default-feature sf2-game tests, the shipping architecture gate, the renderer smoke path, unattended route simulations, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed all three sampled fighter-pose tracks from SF2's three-fighter interception. Each fighter now owns flat typed wave phase, cruise control, combat corridor, pending velocity, movement continuation, and temporary weapon-aim state, while generated semantic actions reproduce banking, player-facing, vertical motion, corridor correction, and the one-boundary hidden presentation. The oracle callback stream is reduced to a compact semantic fixture; its independent replay proves all 1503 retained poses before generation. The old pose tracks compile only as test evidence, and the exhaustive native regression checks every visible pose, the hidden boundary, typed activity, and all departures. Both generator freshness checks, the default build, the shipping architecture gate, all 62 default-feature sf2-game tests, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, register names, or source addresses in shipping logic.
- 2026-07-22: removed all 33 sampled hostile-projectile tracks from SF2's first re-engagement. Each projectile now owns flat typed flight phase and motion-progress state and executes semantic target contraction, exact fixed-point aiming, smooth angle chase, flight, and cruise operations against current, previous, or midpoint player positions. The oracle-only callback capture is reduced to a semantic fixture; the generator independently replays every operation and proves all 942 retained four-frame poses before emitting the native schedule. The old pose tracks are test-only evidence, and an exhaustive native regression compares every live projectile pose and typed phase at every certified boundary. Generator freshness checks, the default build, the shipping architecture gate, and the complete workspace suite pass without segmented memory, generic byte RAM, processor state, or source addresses in shipping logic.
- 2026-07-22: removed all eight sampled hostile-projectile tracks from SF2's three-fighter interception. A resumed retail capture was first proven identical to the accepted fixture across all 832 four-frame sortie samples and the exact 3328/3330 return boundaries. The oracle tracer now classifies slots by their current projectile shape so departed fighter slots reused for lasers cannot contaminate the actor stream. Shipping uses the same flat typed hostile-projectile state and semantic contraction, aiming, flight, and cruise operations as the first re-engagement; only the generated action schedule is encounter-specific. The compact logic import independently replays all 277 retained projectile poses, the old tracks compile only as test evidence, and the exhaustive native regression proves pose plus typed phase at every live boundary.
- 2026-07-22: removed all four sampled actor-pose tracks from SF2's first re-engagement. The two fighters now use flat typed wave, centering, bank, pitch-target, velocity, and cooperative-continuation fields; the two capital craft use typed wave, movement, bank, targeting, and temporary weapon-aim fields. Oracle-only callback imports are reduced to semantic fixtures and generated action schedules, then independently replayed before generation. Native regressions compare every retained four-frame pose across 449 visible fighter boundaries and both complete capital lifetimes, including temporary inactivity and departures. All generated checks, the native architecture gate, and all 59 sf2-game tests pass; shipping code has no source addresses, processor state, segmented memory, or generic byte RAM.
- 2026-07-22: removed the sampled capital-craft pose fallback from the SF2 first sortie after the frame-900 handoff. Both large craft now use flat typed state for flight angles, velocity, altitude-wave phase, split cooperative movement, and temporary weapon aim. The generated semantic schedule contains only gameplay actions and was independently replayed against all 388 retained four-retail-frame samples through frame 2448; the native game regression compares both complete poses at every boundary. Non-neutral player movement now drives the real player-facing behavior instead of being overwritten by the neutral capture. Source addresses, raw object bytes, and tracing remain confined to oracle tooling. The generator check, native architecture gate, default build, complete workspace suite, renderer smoke path, and unattended route simulations all pass.
- 2026-07-22: closed the SF2 Walker movement row. The flat typed controller now reproduces the cartridge's pilot-specific motion profiles, heading spring and velocity easing, launch countdown, signed fixed-point ascent scaling, one-tick delayed ascent, four-step fall acceleration, dead zone, and collision landing. Read-only Mesen stage instrumentation captured both released and held jump profiles at every native boundary; exact native arrays lock every active frame, while ROM-slice oracle tests lock the underlying integrator, acceleration, scaler, and signed halver. Shipping state contains only semantic Walker fields and never exposes source memory or machine registers. The full release workspace suite passes.
- 2026-07-22: removed the sampled fighter-pose fallback from the SF2 first sortie's complete certified encounter window. The two fighters now advance through typed velocity, altitude-wave/centering, maneuver, weapon, logic-credit, and pending cooperative-task state from the frame-480 combat handoff through frame 2448. A read-only Mesen operation trace recovered each four-frame task slice, including two shared-random service completions and the split movement/steering handoff at frames 2416–2436. The native regression compares both full fighter poses at every captured boundary from the first shot onward, locks the shared and final random states, and asserts that the native boundary equals the certified boundary so the sampled override cannot silently return. Shipping code contains semantic enums, constants, and flat struct fields only; source addresses and write tracing remain oracle-only.
- 2026-07-22: made the SF2 Arwing/Walker round trip a shared typed player-form state instead of mission-specific booleans. Select now drives the retail two-stage fold in Eladard, Titania, and Astropolis; all three pilot classes select their own adjacent flight-side/walker-side meshes. Six 17-byte ROM descriptors lock mesh order, seven-frame modulus, endpoints, signed frame direction, initial frame, and final craft shape; a live Fox round trip locks the native four-retail-frame samples and 48/56-frame completion boundaries. Eladard entry and exit now require this player-driven shared state; the Battle Carrier still uses its mission-owned handoff. Walker movement, camera, jump, and blaster are connected. Shipping state is flat domain structs only; descriptor offsets and machine-field write tracing remain in the oracle.
- 2026-07-22: separated SF2 pilot presentation into the retail active-flight, animated transformation, and walker form families. The live flight service selects three shared pilot classes from the table at file offset `0x31692`: Fox/Falco use decoded catalog shape 52, Peppy/Slippy 53, and Miyu/Fay 85. The six-entry transformation and walker tables remain separately typed and ROM-tested. A matching retail frame confirms that the post-handoff view is first-person: shipping state preserves the pilot's flight-craft identity while the render boundary omits the hidden player body. The port remains flat typed Rust; source offsets and live write instrumentation stay in oracle-only tooling.
- 2026-07-22: replaced the SF2 native player's conflated stationary laser with the three retail object lifecycles. Holding B now launches the immediate travelling rapid shot through its four decoded meshes, later creates a collision-disabled muzzle orb, applies the exact six-pilot charge thresholds and observed 15 Hz ready timing, and releases a separate 10-damage charged projectile through its two decoded meshes. Early release cancels the orb, both release paths preserve its three-tick fade, projectile collisions subtract typed attack power before starting the existing destruction timeline, and missed projectiles expire on their observed retail schedules. Shipping state remains flat typed Rust; source addresses and forced-threshold experiments remain in the read-only Mesen oracle.
- 2026-07-22: replaced the reachable SF1 end-level tally stand-in with the retail sequence. A read-only Mesen script drives the Rev 2 cartridge into a deterministic normal clear and records the graph/commit/bonus phases. Shipping Rust now uses a typed phase enum to count by three, defer score mutation, delay threshold bonuses, and award credits at the source boundary. The renderer has a dedicated tally state, retains the stage-clear scene and ordinary flight HUD, and draws the exact source coordinates, FACE.CGX portraits/dead static frames, teammate bars, score graph, total, and bonus message; a framebuffer test prevents regression to the planet-map backdrop. The unrelated unreachable whole-mesh grey wireframe branch was removed, leaving the exact source Face2 wire shapes as the only wireframe path.
- 2026-07-22: replaced the single SF2 opening-sortie backdrop with semantic mission backgrounds. A read-only Mesen helper loads retail mission states, disables objects/subscreen colour math, and captures only the PPU background layer. The renderer now carries exact native textures for deep space, Eladard surface and interior, Titania, and Battle Carrier interiors; Astropolis uses its verified black PPU layer while the coloured exterior/corridor remains ordinary Super FX geometry. The app maps every typed Eladard, Titania, Carrier, and Astropolis phase without source addresses or generic memory. Five source-frame hashes, exhaustive mapping tests, and the real offscreen GPU path pass.
- 2026-07-22: removed the final SF1 color-table dependency from SF2 polygon rendering. A deterministic extractor validates the retail light-pointer aliases and emits SF2's exact four depth banks plus four-by-ten shade rows. Retail differs from SF1 in multiple depth pairs and six light entries; focused differential tests lock those changes. Read-only GSU traces across Eladard surface/interior, Titania, Carrier, and Astropolis verify that live missions select the standard SF2 depth family, while Astropolis directly exercises the SF2 shade table. Shipping code receives only typed generated arrays—no memory image, source pointer, or machine state.
- 2026-07-22: restored exact Super FX palette-pair output without introducing emulated state. Flat faces retain both packed palette nibbles through the GPU; fragment parity is evaluated in the 256x224 source raster so HD scaling preserves the original checker blocks, and a selected color-zero nibble discards before depth writes. Direct 512x448 readbacks prove scaled parity and alternating transparency. The CPU averaging helper remains diagnostic-only.
- 2026-07-22: replaced SF2's opening-sortie-only palette shortcut with typed mission palette selection. A ROM extractor emits all five exact BGR555 catalog rows as ordinary Rust assets. Live retail CGRAM/typed-state traces prove Standard for space, all interiors, Titania, and both Battle Carriers; EladardSurface through the base entrance; AstropolisExterior through base entry; and Standard from each interior transition onward. Shipping state carries only the semantic palette enum—no selector byte, address, or emulated memory.
- 2026-07-22: closed the SF2 polygon-texture gap. Live retail code identifies the descriptor table at file `0x8703`, the layout pointer table at `0x897C`, 12 exact coordinate records, and packed-nibble source banks 12–14. The generator emits 211 typed flat descriptors plus the three pixel banks; all 52 texture IDs referenced by 126 decoded shapes are valid, and the shared GPU renderer now selects SF2 assets instead of reusing SF1 descriptors or emitting debug magenta. A real campaign-missile readback renders multiple sampled colors with zero magenta pixels.
- 2026-07-22: corrected the Fortuna SEA/GROUND fade from an HD polygon-palette lerp to the retail effect. WORLD arms a typed source plus the 30-byte cursor; the shipping NMI path advances it once per game tick; the renderer copies exactly one source color word into background CGRAM row 4 from color 15 down to 1 while preserving color 0 and the independently selected polygon palette in row 7. Restart-fade pointer 62 now decodes to Ground with cursor 30. A direct retail `fadepalto_l` oracle test, real Fortuna composite ownership/recolor tests, shell-path regression, GL runtime tests, and focused workspace checks all pass.
- 2026-07-22: closed the hidden-surface/normal gap in both games. SF1 and SF2 generators now resolve each source visibility selector into a typed vertex triangle; the renderer rejects hidden faces with the retail projected-winding rule, retains two-sided records, and renders every shard during explosions. SF1 authored face normals now drive lighting and explosion direction instead of geometry-derived approximations. Removed the stale runtime Arwing/Great Commander mesh override in favor of all source-generated animation frames; corrected duplicate-header Arwing selection and the nine-frame player-laser macro. Both generators are deterministic, the native architecture gate is green, real SF1/SF2 app captures are nonempty, all three SF1 routes reach their endings unattended, and the full Nix workspace suite passes.
- 2026-07-19: closed the hidden renderer material gap: shape compiler now retains every ShapeHdr color-table pointer; new ROM generator emits all reachable color/animation/texture records; GPU renders exact packed-nibble TEX_01/23 materials with the live shape palette. All-workspace Nix run green, including offscreen texture readback and all three unattended routes.
- 2026-07-10 loop tick 187: enemy_a angle_xz→yanglexy; retail muzzle rotate8 chain MATCH 8/8; ledger 2169/2169.
- 2026-07-10 loop tick 186: goto_WP/GOTOPOS/face* → aim_angle (Yanglexy nega + Xanglexabs); re-bless pi_ponpon; ledger 2169/2169.
- 2026-07-10 loop tick 185: aim_angle→sf-core; Xanglexabs Manhattan vs Xanglexy scaled; hypot pitch→xzdiffs; ledger 2169/2169.
- 2026-07-10 loop tick 184: path P_SPAWN Roffs → `strat_roffs_full_scaled`; rotate_8xz/yz/16yz + full Roffs into sf-core; re-bless pi_tow_0; ledger 2169/2169.
- 2026-07-09 loop tick 130: shieldup wireframe meter color + item6/playermove wire-end; MoveWall make_snd on wall latch; tests wire_shield_movewall + hud; ledger 2169/2169.
- 2026-07-09 loop tick 131: player body/wing damage SE + death BGM $11; nukeexp $30 verified; tests player_damage_sfx (10); ledger 2169/2169.
- 2026-07-09 loop tick 132: HUD arrow $8A drain + dopause PauseSnd $02/$01; tests hud arrow + shell pause; ledger 2169/2169.
- 2026-07-09 loop tick 133: nosetport3 SoundCmd + bird_touch path inline + planets_init clear; ENTERSPEC routechange1; tests nosetport3_bird_touch (4); ledger 2169/2169.
- 2026-07-09 loop tick 129: Laser/Missile/HitWall `make_snd` on fire helpers + boss2/sea/bossb; bomb HUD `specflash` blink; item5→canonical SPECWEPCNT; tests sound_wiring + bomb_flash; ledger 2169/2169.
- 2026-07-09 loop tick 128: Continue credits gate + talk flash frame 4 + EnemyBattry/RingLaser `make_snd` on plasma/oval/ring/yhplasma (+ tank/houdai/boss Hplasma); tests shell/strings/sound_wiring; ledger 2169/2169.
- 2026-07-09 loop tick 127: find_near → `strat_dist_xz` (xzdiffs_l) gate+rank; Y ignored; `retail_find_nearobject_vs_port` MATCH; tests `find_near_xzdiffs.rs` (2); ledger 2169/2169.
- 2026-07-09 loop tick 126: Minor #13 — camera WRAM-writes viewpos*; bgsscrollZ←viewposz; tests `bgsscroll_viewposz.rs` + camera assert; ledger 2169/2169.
- 2026-07-09 loop tick 125: High #3 planet `game_mode=0` + Minor #12 ztilt floor/wall gates; water/space mode tags; tests `planet_yvel_ztilt.rs` (2); ledger 2169/2169.
- 2026-07-09 loop tick 124: barrel-roll `s_beqdec` window + polarity; `set_player_on_field` / onfield view (perc87 X, fixed ViewCY); tests `player_onfield_barrel.rs` (4); ledger 2169/2169.
- 2026-07-09 loop tick 161: enemy_a Minors #9–#14 — bomwing/cameleon drop enemy1; flashplayer no colframe seed; gate bank $7E + touch→spin; explode special→gate2 + inviewpl silent remove (#13 superseded by Medium #36); tests enemy_a_minors_9_14.rs (7).
- 2026-07-09 loop tick 160: enemy_a Minors #6–#8 — zacos muzzle Z120 (weapon_pos+elaserfireZoff); verified clship flyin sflag1 notdelay + zaco2loop HMISSILE1; tests enemy_a_minors_6_8.rs (4).
- 2026-07-09 loop tick 159: enemy_a Minors #3–#5 — tadpole fire `<1500` + zaco1_phase0 `>=1000` fixes; verified item5/mid-band/zaco0 spread; tests enemy_a_minors_3_5.rs (5).
- 2026-07-09 loop tick 158: enemy_a Minors #1–#2 — relslowlaser muzzle Z80 (rotated) + laser colltype; relelaserhome lock |dz|<800; tests enemy_a_minors_1_2.rs (4).
- 2026-07-09 loop tick 157: enemy_a Mediums #34–#37 (hard90yr colltype, delayexplode s_decbpl, pillar3explode 8-child, init fall-through); tests enemy_a_mediums_34_37.rs (8). **enemy_a Mediums closed.**
- 2026-07-09 loop tick 156: enemy_a Mediums #23–#33 (item5/up1man/clship/zaco1/friendexitbase/gate2/skillfly; #25 accepted, #29 superseded); tests enemy_a_mediums_23_33.rs (9).
- 2026-07-09 loop tick 155: enemy_a Mediums #16–#22 (zaco4 fallthrough/leftpl, zaco3die/go, para initface/aim/gravity); tests enemy_a_mediums_16_22.rs (7).
- 2026-07-09 loop tick 154: enemy_a Mediums #11–#15 (zaco2loop/wormgo leftpl, itemtorange, beqdec); tests enemy_a_mediums_11_15.rs (5).
- 2026-07-09 loop tick 153: enemy_a High #10 base1 hit-triggered door FSM (HF1 open/wait/close); tests base1_door.rs (3). enemy_a Highs closed.
- 2026-07-09 loop tick 152: enemy_a High #6–#9 Achase proportional (zaco3/4/parajump/clship) + chase→clshipboost; tests enemy_a_achase_clship.rs (5).
- 2026-07-09 loop tick 151: enemy_a High #5 jmp_higher/jmp_lower worldy half-spaces (gate2/zaco2/zacos/zaco3die/zaco1); tests enemy_a_worldy_higher.rs (5).
- 2026-07-09 loop tick 150: enemy_a High #1/#2 (relslowlaser + notdelay bit-count) + HUD Crit #8/#9 (damage/destruct range SE); tests enemy_a_weapon_explode_se.rs (4).
- 2026-07-09 loop tick 123: player-move Mediums — Y inclusive+BBOTTOM, BOOSTOBJ tag, pfm_wobble Zrotfloat; outdist ease re-tested; ledger 2169/2169.
- 2026-07-09 loop tick 122: shoulder ztilt lean (deg45/3) + houdai/zaco3 `xzdiffs_l` oracle re-cert MATCH; ledger 2169/2169.
- 2026-07-09 loop tick 121: cam yaw FIXED — `rot_y = (outvy-turnrot)>>8` (full getview_l viewrot); ledger still 2169/2169.
- 2026-07-09 loop tick 120: float-ground FIXED — cam pitch/roll from ROM `outvx`/`outvz` (+ `noxrot`/`dozrot`); horizon +18 kept; `getview_viewrot_vs_rom` MATCH.
- 2026-07-09 loop tick 119: ledger 2169/2169 True (child walk + map restart + title/planets/fade + mario show + structural shapes/paths).
- 2026-07-09 loop tick 14: SLSPARK ported (`sgen_slspark`); float-ground chase-feel diagnostic test; REACHABLE_UNPORTED marked done (54/54 registered).
- 2026-07-09 loop tick 13: closed GSU arctan off-axis + msqrt32 high-domain as already-resolved; float-ground diagnosis banked (cam chase-feel vs outvx; +18 stays).
- 2026-07-09 loop tick 12: sr16 achase antipodal fixed (`current+adiv2(target-current)` i16 wrap); 7623/7623 exact. math_helper still 0.
- 2026-07-09 loop tick 11: MCALC_CIRCLE Bresenham ported; MGENUV*/MGENZLIST structural (HD sphere blit). **math_helper False = 0**.
- 2026-07-09 loop tick 10: MSH_ROTPOINTS8 packed MULT formula + 8_16 scale→FMULT; math_helper False down to 5 (circle/UV/Z lists).
- 2026-07-09 loop tick 9: rotproj local offset + mcore_zrot; MSH_X16 mirror; structural UI/path leaves closed. Remaining: 9 GSU mesh/UV/circle.
- 2026-07-09 loop tick 8: SGENSPARK+lspark ported+wired to pcbox wing hits; MSH_ROTPOINTS16 batch helper; ANGLEXY compose closed in tick 7.
- 2026-07-09 loop tick 7: `wmat_rot_point` (mwmatrotp16) bit-exact 91854 vs GSU; ANGLEXY compose helpers (`strat_angle_xz_abs`/`yz`/`yz_abs`) + tests; ANGLEXY_OFF structural (uncalled).
- 2026-07-09 loop tick 6: nucleuswallrot fixed (float sin → rotate_16xz); fuzz 256 exact. CROTMAT16 marked covered by gsu_rotmat.
- 2026-07-09 loop tick 5: `rotate_8xz/yz/yx` + `mulslog_mac8` bit-exact (fuzz_rotate8); gen_weapon muzzle leaves closed.
- 2026-07-09 loop tick 4: `divorcefamily_l` → `Objects::divorce_family` on free/explode; child/mother flags in sf-game.
- 2026-07-09 loop tick 3: camera pull-back keeps FP16.16 fraction (fixes i16 stair-step wobble) and reads chased `outdist`.
- 2026-07-09 loop tick 2: ported `divide_by_num`/`calc_scroll_step` (CALCDXDY leaf); fuzz_calcdxdy 1848 exact.
- 2026-07-09 loop tick 1: restored Claude session logs to `re_loop_sessions/`; ported `rotate_16xz`/`rotate_16yz` + `strat_gen_vecs_3d_scaled` (sr_gen_3dvecs×4) with oracle fuzz (bit-exact); leaf queue in `docs/PORTING_LOOP.md`.
- 2026-07-04 wave 1: trig/player/coldet/strats/boss/mapvm audits — ~18 fixes, all committed (see rom-oracle-plan memory).
- 2026-07-07 wave 2 COMPLETE: path (8), map-VM (5), showview (6), bg2d. 229/0.
- 2026-07-08 wave 11 (enemy/boss tail closed): ALL bosses ported incl. both
  Andross forms (face 1_5 + robot via the new MAP1_6A Venom-surface port) and
  gggy (bossH, wired to its real 1_4 slot); the full reachable enemy tail (~55
  strats: ground artillery, walkers, wireman, missile pods, meteors, mines,
  bases/colonies, volcano/firepillar, doors/walls/trees, iris/truck/item/shou,
  + all 15 clear-demo fleet ships). Registration-consistency audit 96/97 (1
  fixed); ALL reliable placement/index bugs fixed (misstank/kdoor/walls/
  hard180yrfog/gggy-vs-boss2/clship-beqdec). mulslog+achase latent-math fixes vs
  the real cartridge (fuzz harness). Authoritative istrat index-map doc (raw-row
  != port-index corrected). Workspace ~450/0. Residual: base1/kichi2 niche
  caveats, render/audio structural labels, tier-2 retail certification.
- 2026-07-07 wave 10: make_snd positional layer wired (door/sea SFX live, F1-F6 fixed); madtrucker family (route-2 2_6, own swerve/skid death, HP bar) -> boss parity 17/~22; player-move audit banked (3 High feel bugs: boost/brake no-cooldown, yvel 1.25 vs 1.375, planet-init dispatch). Remaining bosses: amoeba, cruiser1/2, Andross(bossB)+bossh.
- 2026-07-07 wave 9: LE_* warp/route dispatch (black-hole/special routes now reachable; strat-side arming still blocked on unported bholeexit); webmonster spider (route-3 3_2) -> boss parity 16/~22. Workspace 310/0. Remaining bosses: amoeba, cruiser1/2, madtrucker, seadragon2-variants, Andross(bossB)+bossh capstones.
- 2026-07-07 wave 8: seadragon/seadragon2/lochness (route-3 3_3, sprouty neck chain) -> boss parity 15/~22; audits banked (sound-IDs: 0 wrong-id/4 wrong-type/2 stray; route-progression: LE_* dispatch missing = warps unreachable). Workspace 301/0.
- 2026-07-07 wave 7: castanet 'Metal Smasher' + shared ground-vehicle base
  (route-2 2_5); chicken + shared arm_istrat grabber (route-3 3_3) -> boss parity
  14/~22; enemy-A ground/common batch (10 High +~27, ROM-correct laser speed/fire
  masks/worldy/Achase across nearly every regular enemy; #31 correctly skipped as
  a bad-doc-fix; base1 door caveated). 6 fixtures re-blessed single-cause
  (RNG-aligned). Workspace 296/0.
- 2026-07-07 wave 6: path friend-weights (weighted RNG tree); pcbox 3-box
  collision-proxy layer (gated); unported-boss roadmap (docs/UNPORTED_BOSSES_PLAN
  .md, 12 ported/~11 unported/2 cut); mulslog bit-exact vs ROM (3 latent >=128
  bugs, 11264-pair oracle proof); flingboss+deadflingboss ported (boss parity
  12/~22, IS 58/59, route-2 2_4). Workspace 284/0.
- 2026-07-07 wave 5: boss1 (12 fixes) + boss8/seamon (15) + score/credits/tally
  (hit-% + bonertab + real HUD score) + makesnd positional SE layer (infra) +
  m_bossHP accumulator (boss bar now drains, 12 sites) + s_test_special count fix.
  Workspace 267/0. Remaining: makesnd call-site wiring, pcbox routing, path
  leftovers, mulslog/speedto >=128 latents, unported bosses (bossB/D3/DSTRATS).
- 2026-07-07 wave 4 (user-report day): opening cinematic restored (view matrix
  direct world->camera + yaw-rotated cull; view_matrix_guard test); death crash
  sequence + lives unification (game-over reachable); explosion/hitflash ranged
  SFX; horizon +18 base restored (load-bearing); title spin fixed (camera
  co-rotation + ROM tit pose); boss2/bossg 9 fixes + BLACK_C; FADETOSEA/GROUND
  palette crossfade. Workspace 248/0.
- 2026-07-07 wave 3 COMPLETE: enemy-B/ground all 26 findings + MOTHER submap ported (asteroid waves restored). Workspace 241/0. Remaining queue: HUD/score, SFX set_sound2 map, pcbox collision, palette fades, path leftovers, remaining boss ticks, >=128 latents.
- 2026-07-10 loop tick 189: camera VIEWTYPE_TOOBJ look-at → aim_angle (nega Xanglexy + Yanglexy + raw outvz); ledger 2169/2169.
- 2026-07-10 loop tick 190: camera pull-back → rotate_16yz/xz mulslog (drop float sin/cos; yaw swings pull-back); ledger 2169/2169.
- 2026-07-10 loop tick 191: bossFC2/FC3 objinfront + playerturn180 + bossFtur fire window verified (AUDIT_ENEMY_B C#1–2, H#12); ledger 2169/2169.
- 2026-07-10 loop tick 192: bossA Criticals #3–#9 verified (GO/IROTATE/husk revive); AUDIT_ENEMY_B Criticals closed; ledger 2169/2169.
- 2026-07-10 loop tick 193: AUDIT_ENEMY_B Highs #10–#19 verified; bossFB/enemy_b COLLTYPE_ENEMY1 0x10 fix; ledger 2169/2169.
- 2026-07-10 loop tick 194: AUDIT_ENEMY_B Mediums #20–#26 verified (`enemy_b_mediums.rs`); ledger 2169/2169.
- 2026-07-10 loop tick 195: AUDIT_ENEMY_B Minors verified (`enemy_b_minors.rs`); AUDIT_ENEMY_B fully closed; ledger 2169/2169.
- 2026-07-10 loop tick 196: AUDIT_BOSS_TICKS2 Highs #1–#4 verified (`boss1_highs.rs`); ledger 2169/2169.
- 2026-07-10 loop tick 197: AUDIT_PLAYER_MOVE High #1 boost/brake gates verified (`player_boost_brake.rs`); player-move Highs closed; ledger 2169/2169.
- 2026-07-10 loop tick 198: AUDIT_ROUTE_PROGRESSION Findings 1–3 verified (LE_* warp dispatch + routechange* + level1_5 mapend7); ledger 2169/2169.
