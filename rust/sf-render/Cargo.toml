[package]
name = "sf-render"
version.workspace = true
edition.workspace = true
license.workspace = true

[dependencies]
sf-core.workspace = true
wgpu = "26"
bytemuck = { version = "1", features = ["derive"] }
pollster = "0.4"
raw-window-handle = "0.6"

[dev-dependencies]
# Headless wgpu adapters are created straight from an Instance in tests
# (no window/surface needed), so the offscreen tests no longer need SDL.
